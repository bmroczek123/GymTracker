generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Workout {
  id       String   @id @default(cuid())
  date     DateTime @default(now())
  exercise String
  weight   Float
  reps     Int
}